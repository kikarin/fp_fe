# Public API base (browser-exposed — never put secrets here)
# Local: http://localhost:1337/api
# Production: https://api.firstpective.com/api
VITE_API_URL=http://localhost:1337/api

# Optional comma-separated media host allowlist (defaults derived from VITE_API_URL + Cloudinary)
# VITE_MEDIA_HOSTS=api.firstpective.com,res.cloudinary.com,localhost
